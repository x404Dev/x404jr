import { ActivityType } from "discord.js";

const BotConfig = {
    BOT_NAME: "x404Jr", // Used in the embed footer for example, you can change it to whatever you want
    BOT_ACTIVITY: "with my food", // The bots activity
    BOT_ACTIVITY_TYPE: ActivityType.Playing, // The bots activity type, can be Playing, Watching, Listening or Streaming
    BOT_STATUS: "dnd" // The bots status, can be online, idle, dnd or invisible
}

export default BotConfig;
